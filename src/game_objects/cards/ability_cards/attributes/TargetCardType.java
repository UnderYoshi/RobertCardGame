package src.game_objects.cards.ability_cards.attributes;

public enum TargetCardType {
    FRIENDLY,
    ENEMY,
    ALL_FRIENDLY, // No restrictions as to backline, frontline, etc
    ALL_ENEMY, // No restrictions as to backline, frontline, etc
    ALL
}
